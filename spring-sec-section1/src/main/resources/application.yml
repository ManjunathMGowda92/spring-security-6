spring:
  application:
    name: ${SPRING_APP_NAME:spring-sec-section1}
  security:
    user:
      name: ${SECURITY_NAME:fourStack}
      password: ${SECURITY_PWD:BlueMonk@1563}

logging:
  pattern:
    console: "%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n"
  level:
    org.springframework.security : ${SPRING_SECURITY_LOG_LEVEL:TRACE}
